# Очередь задач и отдельный процесс-воркер

Генерация одной Статьи — это многошаговый пайплайн (аутлайн → черновик → рерайт → источники) плюс обложка, порядка 10+ вызовов внешних API и минуты работы. Хендлер aiogram не переживёт рестарт деплоя или падение процесса — работа потеряется без следов. Вместо синхронной генерации внутри хендлера бота ввели таблицу задач в БД (`queued/running/failed/done`, счётчик попыток, идемпотентность) и отдельный процесс-воркер, который её разбирает. Бот только ставит задачи в очередь и уведомляет о результате.
